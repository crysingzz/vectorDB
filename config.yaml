gigachat:
  model: "EmbeddingsGigaR"
  credentials: "${GIGACHAT_CREDENTIALS}"  # задайте переменную окружения GIGACHAT_CREDENTIALS
  verify_ssl_certs: false

database:
  path: "./db"
  collection_name: "documents"

search:
  top_k: 3
  min_score: 0.5
